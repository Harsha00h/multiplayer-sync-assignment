/**
 * The client's view of the wire protocol — the exact same file the server imports.
 * One definition of the wire, two consumers; drift is impossible by construction.
 */
export * from '../../shared/protocol.js';
